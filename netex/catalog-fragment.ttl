# Catalog fragment for the NeTEx/Transmodel EV-charging subset.
#
# This does NOT redefine the whole catalog — the mobility pipeline's catalog.ttl
# already declares eona:catalog (dcat:Catalog) with its title/description. This
# fragment only MERGES additional triples into that single shared catalog via the
# default-graph union (rebuild_default_graph unions ALL named graphs):
#   - one extra dcterms:hasPart edge pointing at the NeTEx scheme, and
#   - the scheme's title/label so Prez renders it as a collection, and
#   - the netex: vann prefix binding for Prez curie routing.
# Loaded (local=True) into its own named graph:
#   https://vocab.eona-x.eu/catalog/netex

@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix skos: <http://www.w3.org/2004/02/skos/core#> .
@prefix vann: <http://purl.org/vocab/vann/> .
@prefix eona: <https://vocab.eona-x.eu/> .
@prefix netex: <https://vocab.eona-x.eu/netex/> .

# Prefix binding for Prez curie routing (so netex:ev-charging resolves).
# Prez expects vann declarations on blank-node subjects, not the namespace IRI.
[] vann:preferredNamespacePrefix "netex" ;
    vann:preferredNamespaceUri "https://vocab.eona-x.eu/netex/" .

# Merge the NeTEx scheme into the shared catalog as an additional member.
eona:catalog dcterms:hasPart netex:ev-charging .

# Display title/label so Prez surfaces the scheme as a collection.
netex:ev-charging a skos:ConceptScheme ;
    dcterms:title "NeTEx/Transmodel — EV charging (subset)" ;
    rdfs:label "NeTEx/Transmodel — EV charging (subset)" .
